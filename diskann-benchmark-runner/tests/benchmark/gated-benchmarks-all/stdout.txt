Registered Benchmarks:
    dim-bench:
        tag "test-input-dim"
        matches all
    exact-type-bench-f32-1000:
        tag "test-input-types"
        float32, dim=1000
    simple-bench:
        tag "test-input-dim"
        dim=None only
    type-bench-f32:
        tag "test-input-types"
        float32
    type-bench-i8:
        tag "test-input-types"
        int8
    another-gated-bench (requires the features "gated-feature-0" and "gated-feature-1"):
        tag "test-input-dim"
        Another gated benchmark
    fully-gated-bench (requires the features "gated-feature-2" or "gated-feature-3"):
        tag "phantom-input"
        A fully gated benchmark
    gated-bench (requires the feature "gated-feature-0"):
        tag "test-input-types"
        A gated benchmark
    something-else (requires the feature "gated-feature-3"):
        tag "sample-input"
        A gated input that always registers its input
    something-else-v3 (requires the feature "gated-feature-3"):
        tag "sample-input"
        A gated input that always registers its input - a redundant registration.