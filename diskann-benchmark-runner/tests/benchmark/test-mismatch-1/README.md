Mismatch diagnostics for ExactTypeBench description paths.

Job 1: float16 with dim=1000 — ExactTypeBench fails on type only (dim matches).
Job 2: float16 with dim=128 — ExactTypeBench fails on both type and dim.
