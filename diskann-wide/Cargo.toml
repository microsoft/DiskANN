[package]
name = "diskann-wide"
version.workspace = true
description.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
edition = "2024"

[lints]
clippy.undocumented_unsafe_blocks = "warn"
clippy.uninlined_format_args = "allow"

# different from workspace level for slightly better compatibility with the `alias!` macro.
clippy.allow_attributes = "allow"

[dev-dependencies]
paste.workspace = true
rand.workspace = true
half = { workspace = true, features = ["rand_distr", "num-traits", "bytemuck"] }
bytemuck = { workspace = true, features = ["must_cast"] }

[dependencies]
cfg-if.workspace = true
half.workspace = true
