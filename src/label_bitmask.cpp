#include "label_bitmask.h"

// simple_bitmask's methods are defined inline in label_bitmask.h (header-only), so
// this translation unit intentionally contains no out-of-line definitions. It is
// retained because build project source lists (the diskann DLL and consumers such
// as AdsSnr's ANNTestTool) reference it.
namespace diskann
{
}
