set(CMAKE_CXX_STANDARD 14)


add_executable(truncate_rnsg truncate_rnsg.cpp)
if(MSVC)
	target_link_options(truncate_rnsg PRIVATE /MACHINE:x64)
	target_link_libraries(truncate_rnsg debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(truncate_rnsg optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
else() 
	target_link_libraries(truncate_rnsg ${PROJECT_NAME} -ltcmalloc)
endif()


add_executable(test_partitioning test_partitioning.cpp)
if(MSVC)
	target_link_options(test_partitioning PRIVATE /MACHINE:x64)
	target_link_libraries(test_partitioning debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(test_partitioning optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
else() 
	target_link_libraries(test_partitioning ${PROJECT_NAME} -ltcmalloc)
endif()

add_executable(calc_recall calc_recall.cpp)
if(MSVC)
	target_link_options(calc_recall PRIVATE /MACHINE:x64)
	target_link_libraries(calc_recall debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(calc_recall optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

add_executable(opt_disk_graph opt_disk_graph.cpp)
if(MSVC)
	target_link_options(opt_disk_graph PRIVATE /MACHINE:x64)
	target_link_libraries(opt_disk_graph debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(opt_disk_graph optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

add_executable(fvecs_to_bin fvecs_to_bin.cpp)
if(MSVC)
	target_link_options(fvecs_to_bin PRIVATE /MACHINE:x64)
	target_link_libraries(fvecs_to_bin debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(fvecs_to_bin optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

add_executable(ivecs_to_bin ivecs_to_bin.cpp)
if(MSVC)
	target_link_options(ivecs_to_bin PRIVATE /MACHINE:x64)
	target_link_libraries(ivecs_to_bin debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(ivecs_to_bin optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

add_executable(int8_to_float int8_to_float.cpp)
if(MSVC)
	target_link_options(int8_to_float PRIVATE /MACHINE:x64)
	target_link_libraries(int8_to_float debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(int8_to_float optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

add_executable(uint32_to_uint8 uint32_to_uint8.cpp)
if(MSVC)
	target_link_options(uint32_to_uint8 PRIVATE /MACHINE:x64)
	target_link_libraries(uint32_to_uint8 debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(uint32_to_uint8 optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

add_executable(nsg_bfs nsg_bfs.cpp)
if(MSVC)
	target_link_options(nsg_bfs PRIVATE /MACHINE:x64)
	target_link_libraries(nsg_bfs debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(nsg_bfs optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

add_executable(gen_random_slice gen_random_slice.cpp)
if(MSVC)
	target_link_options(gen_random_slice PRIVATE /MACHINE:x64)
	target_link_libraries(gen_random_slice debug ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_DEBUG}/nsg_lib.lib)
	target_link_libraries(gen_random_slice optimized ${CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE}/nsg_lib.lib)
endif()

# formatter
add_custom_command(TARGET calc_recall PRE_BUILD COMMAND clang-format-4.0 -i ../../../include/*.h ../../../include/dll/*.h ../../../src/*.cpp  ../../../tests/*.cpp ../../../src/dll/*.cpp ../../../tests/utils/*.cpp)
